[1.1.6] 加载表结构机制优化
1、打开程序取SQLLite中的表结构，不调取数据库
2、取消表结构更新的时候，更新进程还在跑，需要取消更新进程
3、取消更新表结构后，现有的表结构应该不变
4、更新表结构弹窗支持选择过滤表数据为空的表格
[1.1.7] 界面优化
1、菜单左侧留白过多
2、数据查询中，表的标签页，支持右键关闭全部、关闭其他、关闭左侧、关闭右侧
3、支持右键表空间树节点，删除表空间下的所有子表
4、搜索表的时候自动展开有匹配表的表空间
[1.1.8]
1、数据查询，执行按钮不要根据表格宽度跑到最右侧
2、查询条件下拉框支持输入字符模糊匹配
3、检查执行查询的时候，会不会自动做分页，防止表数据太多一次性查询出来会卡死
4、移除数据查询界面的数据源切换下拉，后续在数据源管理界面中切换
[2.0.0] 项目化管理重构
1、全新项目管理模块，支持多个项目独立管理
2、每个项目独立配置数据源（Oracle/达梦数据库）
3、每个项目独立配置API和Redis信息
4、表结构统一缓存，所有项目共用（表结构一致，数据不同）
5、简化数据模型：
   - projects表：项目基本信息
   - data_sources表：每个项目一个数据源（1:1关系）
   - project_configs表：每个项目独立配置
6、三标签页设计：项目 / 数据源 / API & Redis
7、数据源和API配置页面顶部显示当前项目名称
8、数据库迁移逻辑：自动检测并迁移旧版本数据库，保留 schema_cache 数据
9、数据源保存优化：自动检测已有数据源，执行更新而非重复创建
10、全局数据源口径统一：所有操作统一使用当前激活项目的数据源
